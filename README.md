# RAG-knowledge-assistant

## Run

Use the project through `uv` so the declared dependencies are available:

```bash
uv run python -m src.rag_pipeline
```

Do not run the project with plain `python3 -m src.rag_pipeline` unless you have separately installed the dependencies into that interpreter.
